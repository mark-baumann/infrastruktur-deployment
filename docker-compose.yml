# AUTO-GENERATED — nicht manuell bearbeiten
# Quelle: config/services.yaml  |  Generator: scripts/render_configs.py

services:

  vergleich-agenten:
    image: ghcr.io/mark-baumann/vergleichs-ki:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8501/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  finanz-agenten:
    image: ghcr.io/mark-baumann/finanz-assistent:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8502/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  handels-agenten:
    image: ghcr.io/mark-baumann/handels-agenten:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8503/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  ki-lernplattform:
    image: ghcr.io/mark-baumann/ki-lernplattform:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8505/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  spam-klassifikation:
    image: ghcr.io/mark-baumann/spam-klassifikation:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8510/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  anonymisierungs-pipeline:
    image: ghcr.io/mark-baumann/anonymisierungs-pipeline:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8511/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  dokumenten-agent:
    image: ghcr.io/mark-baumann/rag-agent-langgraph:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8512/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  aktienanalyse:
    image: ghcr.io/mark-baumann/taegliche-aktienanalyse:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8513/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  ebay-agent:
    image: ghcr.io/mark-baumann/ebay-scraping-agent:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8514/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  browser-nutzung:
    image: ghcr.io/mark-baumann/browser-nutzung:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8515/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  open-manus:
    image: ghcr.io/mark-baumann/open-manus:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8516/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  verstaerkungslernen:
    image: ghcr.io/mark-baumann/agenten-verstaerkungslernen:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8517/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  art-agent:
    image: ghcr.io/mark-baumann/ART:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8519/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  status-dashboard:
    image: ghcr.io/mark-baumann/status-dashboard:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    networks: [edge]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request;urllib.request.urlopen('http://localhost:8520/_stcore/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${TUNNEL_TOKEN}
    networks: [edge]

networks:
  edge:
    driver: bridge
