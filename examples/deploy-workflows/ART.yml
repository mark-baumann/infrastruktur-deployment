# .github/workflows/deploy.yml — generiert für: ART-Agent
# Kopiere diese Datei in: ART/.github/workflows/deploy.yml

name: Deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    uses: mark-baumann/infrastruktur-deployment/.github/workflows/build-deploy.yml@master
    with:
        service_name: art-agent
        port: "8519"
        app_file: app/app.py
        healthcheck_type: http
    secrets: inherit
