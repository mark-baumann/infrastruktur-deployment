# .github/workflows/deploy.yml — generiert für: Anonymisierung
# Kopiere diese Datei in: anonymisierungs-pipeline/.github/workflows/deploy.yml

name: Deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    uses: mark-baumann/infrastruktur-deployment/.github/workflows/build-deploy.yml@master
    with:
        service_name: anonymisierungs-pipeline
        port: "8511"
        app_file: app/app.py
        healthcheck_type: http
    secrets: inherit
