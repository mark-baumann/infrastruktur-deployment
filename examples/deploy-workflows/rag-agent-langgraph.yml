# .github/workflows/deploy.yml — generiert für: Dokumenten-Agent
# Kopiere diese Datei in: rag-agent-langgraph/.github/workflows/deploy.yml

name: Deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    uses: mark-baumann/infrastruktur-deployment/.github/workflows/build-deploy.yml@master
    with:
        service_name: dokumenten-agent
        port: "8512"
        app_file: app/app.py
        healthcheck_type: http
    secrets: inherit
