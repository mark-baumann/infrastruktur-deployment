# .github/workflows/deploy.yml — generiert für: OCR-Agent
# Kopiere diese Datei in: ocr_recognition_nn/.github/workflows/deploy.yml

name: Deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    uses: mark-baumann/infrastruktur-deployment/.github/workflows/build-deploy.yml@master
    with:
        service_name: ocr-agent
        port: "8518"
        app_file: app/app.py
        healthcheck_type: http
    secrets: inherit
