# .github/workflows/deploy.yml — generiert für: Vergleich-Agenten
# Kopiere diese Datei in: vergleichs-ki/.github/workflows/deploy.yml

name: Deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    uses: mark-baumann/infrastruktur-deployment/.github/workflows/build-deploy.yml@master
    with:
        service_name: vergleich-agenten
        port: "8501"
        app_file: app/app.py
        healthcheck_type: http
    secrets: inherit
