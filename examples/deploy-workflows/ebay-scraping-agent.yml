# .github/workflows/deploy.yml — generiert für: eBay-Agent
# Kopiere diese Datei in: ebay-scraping-agent/.github/workflows/deploy.yml

name: Deploy

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  deploy:
    uses: mark-baumann/infrastruktur-deployment/.github/workflows/build-deploy.yml@master
    with:
        service_name: ebay-agent
        port: "8514"
        app_file: app/app.py
        healthcheck_type: http
    secrets: inherit
